# cinemabook-springboot
CinemaBook — A Spring Boot web app for booking cinema tickets. Covers user management, movies, shows, bookings, payments, and feedback. Uses CSV files as the data store instead of a database.
